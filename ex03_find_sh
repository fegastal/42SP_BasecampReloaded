Escreva uma linha de comando que procure todos os nomes de ficheiros que terminem com ".sh" (sem
aspas) no directório actual e em todos os seus subdirectórios. Deve
exibir apenas os nomes dos ficheiros sem o .sh.

#!/bin/sh
find . -type f -name '*.sh' -execdir basename {} .sh ';'
